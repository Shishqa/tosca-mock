tosca_definitions_version: tosca_simple_yaml_1_3

metadata:
  template_name: "nginx-profile"
  template_author: "shishqa"
  template_version: "1.0"

node_types:

  Nginx:
    derived_from: tosca.nodes.WebServer